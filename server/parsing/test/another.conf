server {
	listen localhost:80;
	server_name localhost default_server;
	root /tmp;
	location / {
		index index.html;
		method GET HEAD POST TRACE OPTIONS;
		cgi_extension php;
		autoindex on;
		client_max_body_size 8k;
	}
}
