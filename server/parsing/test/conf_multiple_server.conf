server {
	listen 80 localhost;
	server_name localhost;
	root /tmp;
	location / {
		method GET;
		index a;
	}
}
server {
	listen 80 localhost;
	server_name 127.0.0.1;
	root /tmp;
	location / {
		method GET;
		index index.php;
	}
}
